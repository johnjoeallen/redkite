<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <!-- redkite:pin-summary project: 16 redkite pin(s) (0 user pinned) — this file: 16 redkite pin(s) (0 user pinned) -->
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.redkite</groupId>
  <artifactId>red-kite-parent</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <modules>
    <module>red-kite-core</module>
    <module>red-kite-maven</module>
    <module>red-kite-metadata</module>
    <module>red-kite-server</module>
  </modules>
  <properties>
    <java.version>17</java.version>
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <spring-boot-maven-plugin.version>3.5.16</spring-boot-maven-plugin.version>
    <maven-compiler-plugin.version>3.16.0</maven-compiler-plugin.version>
    <maven-surefire-plugin.version>3.6.0</maven-surefire-plugin.version>
  </properties>
  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-maven-plugin</artifactId>
          <version>${spring-boot-maven-plugin.version}</version>
          <configuration>
            <skip>true</skip>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${maven-compiler-plugin.version}</version>
          <configuration>
            <release>${maven.compiler.release}</release>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${maven-surefire-plugin.version}</version>
          <configuration>
            <useModulePath>false</useModulePath>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
  <dependencyManagement>
    <dependencies>
      <!-- redkite:dependency-management pin groupId="org.springframework" artifactId="spring-core" version="6.2.19" reason="Enforcer dependency convergence fix by RedKite" — remove this comment to prevent RedKite managing this dependency -->
      <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-core</artifactId>
        <version>6.2.19</version>
      </dependency>
      <!-- redkite:dependency-management pin groupId="org.springframework.retry" artifactId="spring-retry" version="2.0.13" reason="Enforcer dependency convergence fix by RedKite" — remove this comment to prevent RedKite managing this dependency -->
      <dependency>
        <groupId>org.springframework.retry</groupId>
        <artifactId>spring-retry</artifactId>
        <version>2.0.13</version>
      </dependency>
      <!-- redkite:dependency-management pin groupId="org.springframework.cloud" artifactId="spring-cloud-function-context" version="4.3.4" reason="Enforcer dependency convergence fix by RedKite" — remove this comment to prevent RedKite managing this dependency -->
      <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-function-context</artifactId>
        <version>4.3.4</version>
      </dependency>
      <!-- redkite:dependency-management pin groupId="org.springframework.boot" artifactId="spring-boot-autoconfigure" version="3.5.16" reason="Enforcer dependency convergence fix by RedKite" — remove this comment to prevent RedKite managing this dependency -->
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-autoconfigure</artifactId>
        <version>3.5.16</version>
      </dependency>
      <!-- redkite:dependency-management pin groupId="org.apache.httpcomponents.core5" artifactId="httpcore5-h2" version="5.4.3" reason="Enforcer dependency convergence fix by RedKite" — remove this comment to prevent RedKite managing this dependency -->
      <dependency>
        <groupId>org.apache.httpcomponents.core5</groupId>
        <artifactId>httpcore5-h2</artifactId>
        <version>5.4.3</version>
      </dependency>
      <!-- redkite:dependency-management pin groupId="org.apache.logging.log4j" artifactId="log4j-api" version="2.26.1" reason="Enforcer dependency convergence fix by RedKite" — remove this comment to prevent RedKite managing this dependency -->
      <dependency>
        <groupId>org.apache.logging.log4j</groupId>
        <artifactId>log4j-api</artifactId>
        <version>2.26.1</version>
      </dependency>
      <!-- redkite:dependency-management pin groupId="org.apache.tomcat.embed" artifactId="tomcat-embed-core" version="10.1.60" reason="Enforcer dependency convergence fix by RedKite" — remove this comment to prevent RedKite managing this dependency -->
      <dependency>
        <groupId>org.apache.tomcat.embed</groupId>
        <artifactId>tomcat-embed-core</artifactId>
        <version>10.1.60</version>
      </dependency>
      <!-- redkite:dependency-management pin groupId="com.rabbitmq" artifactId="amqp-client" version="5.36.0" reason="Enforcer dependency convergence fix by RedKite" — remove this comment to prevent RedKite managing this dependency -->
      <dependency>
        <groupId>com.rabbitmq</groupId>
        <artifactId>amqp-client</artifactId>
        <version>5.36.0</version>
      </dependency>
      <!-- redkite:dependency-management pin groupId="org.springframework.boot" artifactId="spring-boot" version="3.5.16" reason="Enforcer dependency convergence fix by RedKite" — remove this comment to prevent RedKite managing this dependency -->
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot</artifactId>
        <version>3.5.16</version>
      </dependency>
      <!-- redkite:dependency-management pin groupId="ch.qos.logback" artifactId="logback-core" version="1.6.3" reason="Enforcer dependency convergence fix by RedKite" — remove this comment to prevent RedKite managing this dependency -->
      <dependency>
        <groupId>ch.qos.logback</groupId>
        <artifactId>logback-core</artifactId>
        <version>1.6.3</version>
      </dependency>
      <!-- redkite:dependency-management pin groupId="org.springframework" artifactId="spring-web" version="6.2.19" reason="Enforcer dependency convergence fix by RedKite" — remove this comment to prevent RedKite managing this dependency -->
      <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-web</artifactId>
        <version>6.2.19</version>
      </dependency>
      <!-- redkite:dependency-management pin groupId="org.springframework" artifactId="spring-webmvc" version="6.2.19" reason="Enforcer dependency convergence fix by RedKite" — remove this comment to prevent RedKite managing this dependency -->
      <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-webmvc</artifactId>
        <version>6.2.19</version>
      </dependency>
      <!-- redkite:dependency-management pin groupId="org.springframework" artifactId="spring-expression" version="6.2.19" reason="Enforcer dependency convergence fix by RedKite" — remove this comment to prevent RedKite managing this dependency -->
      <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-expression</artifactId>
        <version>6.2.19</version>
      </dependency>
      <!-- redkite:dependency-management pin groupId="org.springframework.data" artifactId="spring-data-commons" version="3.5.13" reason="Enforcer dependency convergence fix by RedKite" — remove this comment to prevent RedKite managing this dependency -->
      <dependency>
        <groupId>org.springframework.data</groupId>
        <artifactId>spring-data-commons</artifactId>
        <version>3.5.13</version>
      </dependency>
      <!-- redkite:dependency-management pin groupId="org.springframework.amqp" artifactId="spring-amqp" version="3.2.12" reason="Enforcer dependency convergence fix by RedKite" — remove this comment to prevent RedKite managing this dependency -->
      <dependency>
        <groupId>org.springframework.amqp</groupId>
        <artifactId>spring-amqp</artifactId>
        <version>3.2.12</version>
      </dependency>
      <!-- redkite:dependency-management pin groupId="org.apache.commons" artifactId="commons-lang3" version="3.20.0" reason="Enforcer dependency convergence fix by RedKite" — remove this comment to prevent RedKite managing this dependency -->
      <dependency>
        <groupId>org.apache.commons</groupId>
        <artifactId>commons-lang3</artifactId>
        <version>3.20.0</version>
      </dependency>
    </dependencies>
  </dependencyManagement>
</project>
